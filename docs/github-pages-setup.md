# GitHub Pages Setup Guide

This project can be hosted as a free static website with GitHub Pages.

## Steps

1. Open your GitHub repository.
2. Click `Settings`.
3. Click `Pages` in the left menu.
4. Under `Build and deployment`, choose `Deploy from a branch`.
5. Select branch: `main`.
6. Select folder: `/root`.
7. Click `Save`.

After a short time, GitHub will provide a public website URL.

## Korean Guide

이 프로젝트는 백엔드 서버가 필요 없는 정적 웹앱입니다.

GitHub Pages를 켜면 `index.html` 파일이 웹사이트처럼 열립니다.

즉, 사용자는 저장소를 다운로드하지 않아도 웹 주소만 열어서 프롬프트 생성기를 사용할 수 있습니다.
